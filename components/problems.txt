✓1.In items.js the filtering method doesnt work, for example users cant tpye /H to filter
  the extensions and only see those which start with "H". Solved✓

2.The blockqoute started working when i replaced:

editor.chain().focus().deleteRange(range).setNode("blockqoute").run();

with:

editor.chain().focus().deleteRange(range).toggleBlockquote().run();

but i cant understand why we use different methods for codeblock and blockqoute.

✓3.Toggle button group on bubble menu dont listen to the style of the text and act
  independently, for example if u select a part of the text which is already bold, the
  bold button is not highlighted. Solved✓

4.Ordered list doesnt toggle with backspace, when a user pushes the "backspace" button on
  a new empty numbered line instead of the number and the indentation being omitted only(untoggling the ordered list on that line),
  the editor goes up one line, to the end of the previous numbered line, and if they press enter again the next numbered line
  gets generated.

5.The image is not centered yet the styles of the image-wrapper and img itself are 
  applied and it seems like it should be centered but its not, this happened when The
  MUI Joy style was applied to the slash command menu component.

✓6.Later on for the slash command menu we can use the sticky item from "List" in MUI
  Joy to include categories (subheaders) and subitems. Solved✓

✓7.Arrow keys do navigate through the menu but they dont scroll through the list. Solved✓

✓8.Codeblock to Codeblock lowlight. Solved✓

9.Url inserting for link and image is neither user-friendly nor nice, 
notion's url input is nice, try to make it like that

10.material icons doesnt provide an icon for headings, image, ...

